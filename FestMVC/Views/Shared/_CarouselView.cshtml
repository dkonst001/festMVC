@model FestMVC.Models.CarouselImagesViewModel
@{
    ViewBag.Title = "Media Carousel";
    var first = true;
}
<div id=@("myCarousel"+Model.CarouselId) class="carousel slide" data-ride="carousel" data-interval="false">
    <!-- Indicators -->
    @*<ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
            <li data-target="#myCarousel" data-slide-to="3"></li>
        </ol>*@

    <!-- Wrapper for slides -->
    <div class="carousel-inner" role="listbox">
        @foreach (var image in Model.Images)
        {

            if (first)
            {
                first = false;
                <div class="item active">
                    <img src="@Url.Content(Path.Combine("~", @image.ToString()))" alt="Images Don't Exist for this Category" class="img-circle img-thumbnail" />
                </div>
            }
            else
            {
                <div class="item">
                    <img src="@Url.Content(Path.Combine("~", @image.ToString()))" alt="Images Don't Exist for this Category" class="img-circle img-thumbnail" />
                </div>
            }
        }
    </div>

    <!--Left and right controls-->

    <a class="left carousel-control" href=@("#myCarousel"+Model.CarouselId) role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href=@("#myCarousel"+Model.CarouselId) role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>


@*<div class='row'>
        <div class='col-md-12'>
            <div class="carousel slide media-carousel" id="media">
                <div class="carousel-inner">
                    @{
                        var list = Model.ProdDetails as IList<ProductDetails>;
                        var items = list.Take(6);
                        var iterations = 0;
                    }
                    @while (items.Count() > 0)
                    {
                        var active = iterations == 0 ? "active" : "";

                        <div class="item @active">
                            <div class="row">
                                @foreach (var item in items)
                                {
                                    <div class="col-md-2">
                                        <a class="thumbnail" href="#"><img src="data:image;base64,@System.Convert.ToBase64String(@item.ThumbNailImage)" width="100" height="100"></a>
                                    </div>
                                }
                            </div>
                        </div>

                        iterations++;
                        items = list.Skip(6 * iterations).Take(6);
                    }
                </div>
                <a data-slide="prev" href="#media" class="left carousel-control">‹</a>
                <a data-slide="next" href="#media" class="right carousel-control">›</a>
            </div>
        </div>
    </div>*@

